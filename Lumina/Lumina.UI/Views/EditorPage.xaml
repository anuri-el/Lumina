<Page x:Class="Lumina.UI.Views.EditorPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Lumina.UI.ViewModels"
        xmlns:local="clr-namespace:Lumina.UI.Views"
        mc:Ignorable="d"
        Title="EditorPage" Height="450" Width="800">
    <!--
    <Page.DataContext>
        <vm:EditorViewModel />
    </Page.DataContext>
    -->

    <Grid Background="#eeeeee">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="#efefef" Grid.Column="0" >
            <TextBlock Text="Інструменти" FontWeight="Bold" Margin="0 0 0 8"/>
            <Button Content="Save" Command="{Binding SaveCommand}" />
            <Button Content="Rotate" Command="{Binding RotateCommand}"/>
            <Button Content="Scale" Command="{Binding ScaleCommand}"/>

            <Button Content="Normal" Click="Normal_Click"/>
            <Button Content="Edit" Click="Edit_Click"/>
            <Button Content="Preview" Click="Preview_Click"/>
            
            <Button Content="Duplicate Layer" Command="{Binding Layers.DuplicateLayerCommand}" />
            <Button Content="Blur" Command="{Binding Layers.ApplyBlurCommand}" />
            <Button Content="Grayscale" Command="{Binding Layers.ApplyGrayscaleCommand}" />
            <Button Content="Sepia" Command="{Binding Layers.ApplySepiaCommand}" />

            <Separator/>

            <Button Content="Undo" Click="Undo_Click"/>
            <Button Content="Redo" Click="Redo_Click"/>

        </StackPanel>

        <Canvas Grid.Column="1" Background="White">
            <TextBlock x:Name="InfoText" Text="No image loaded" Margin="5,5,0,0" TextAlignment="Center"/>
            <Image x:Name="PreviewImage" Width="400" Height="300" Stretch="Uniform" Margin="5,25,0,0"/>
        </Canvas>

        <Border BorderBrush="Gray" BorderThickness="1" Padding="10">
            <ListBox x:Name="LogBox" Margin="190,320,-610,-10"/>
        </Border>

    </Grid>
    
    <!-- 

    <Grid Background="#eeeeee">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="#efefef" Grid.Column="0" Margin="5">
            <TextBlock Text="Tools" FontWeight="Bold" Margin="0 0 0 8"/>

            <Button Content="Normal" Command="{Binding Tools.NormalCommand}" />
            <Button Content="Edit" Command="{Binding Tools.EditCommand}" />
            <Button Content="Preview" Command="{Binding Tools.PreviewCommand}" />

            <Button Content="Rotate" Command="{Binding Tools.RotateCommand}" />
            <Button Content="Scale" Command="{Binding Tools.ScaleCommand}" />
            <Button Content="Save" Command="{Binding Tools.SaveCommand}" />

            <TextBlock Text="Layers / Effects" FontWeight="Bold" Margin="10 20 0 5"/>
            <Button Content="Duplicate Layer" Command="{Binding Layers.DuplicateLayerCommand}" />
            <Button Content="Blur" Command="{Binding Layers.ApplyBlurCommand}" />
            <Button Content="Grayscale" Command="{Binding Layers.ApplyGrayscaleCommand}" />
            <Button Content="Sepia" Command="{Binding Layers.ApplySepiaCommand}" />
        </StackPanel>

        <Canvas Grid.Column="1" Background="White">
            <Image x:Name="PreviewImage" Width="400" Height="300" Stretch="Uniform" Margin="5"/>
        </Canvas>

        <Border BorderBrush="Gray" BorderThickness="1" Padding="5" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="780" Height="100" Margin="10">
            <ListBox ItemsSource="{Binding Tools.LogText.Split('\n')}" />
        </Border>
    </Grid>
    -->
</Page>
